from django.urls import path
from . import views

urlpatterns = [
    # path('', views.login, name="login"),
    # path('sku_modal', views.sku_model, name="sku_model"),
    # path('logout', views.logout, name="logout"),
    # path('password_reset', views.password_reset, name="password_reset"),
]
